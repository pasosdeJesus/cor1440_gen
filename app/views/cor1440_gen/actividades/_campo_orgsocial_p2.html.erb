<% # f es el formulario que se presenta %>
<% # orgsocialesdisp son los Msip::Orgsocial disponibles para elegir %>
<% lids = orgsocialesdisp.map(&:id) | f.object.orgsocial_ids %>
<% a2 = Msip::Orgsocial.joins(:grupoper).where(id: lids).
  order('msip_grupoper.nombre') %>
<%= f.association :orgsocial, 
  collection: a2,
  label_method: :presenta_nombre, 
  value_method: :id,
  input_html: { class: 'chosen-select' }
%>
