<% if defined? con_label %>
  <% l = con_label ? nil : false %>
<% else %>
  <% l = nil %>
<% end %>
<%= f.association :oficina,
  collection: current_usuario.oficina_id ? [current_usuario.oficina] : 
    Sip::ModeloHelper.coleccion_basica(Sip::Oficina.all, f.object.oficina_id),
  label_method: :presenta_nombre, 
  value_method: :id,
  include_blank: false,
  label: l,
  input_html: { class: 'chosen-select' }
%>
