<% col = Cor1440Gen::Actividadarea.all %>
<%= f.input :busactividadareas,
  collection: col.order(:nombre),
  include_blank: true,
  input_html: {
    class: 'tom-select',
    'data-action': 'change->msip--enviarautomatico#enviar'
  },
  label: false,
  label_method: :nombre,
  selected: Msip::ModeloHelper.poromision(params, :busactividadareas),
  value_method: :id
%>
